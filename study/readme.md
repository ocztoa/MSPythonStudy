## 🤣변수의 종류

---

- #### int : 10, 20, -20, 0 [정수]
- #### float : 1.4, 3.14 [소수]
- #### str : "냠냠", '굿' [문자]

<br>

## 🤣 입력

---

- #### a = input() - 입력
- #### a = int(input()) - 정수 입력
- #### c = int(input()) -
- #### d = int(input()) -여러 줄 입력
- #### a, b = map(int, input().split()) [정수]
- #### a, b, c, d = map(str, input().split()) [문자]

<br>

## 🤣 출력

---

- #### print("냠냠굿")

```
냠냠굿
```

- #### print(10 + 13)

```
23
```

- #### print(13, "hello", sep='-') -> 13-hello
- #### print("1515", end='\n') -> 1515
- #### print("1515", end=' ') -> 1515
- #### print("15") -> 15
- #### print("13" + "5") ->135

<br/>

# 2.5 화면 출력

> print 함수를 사용하여 화면에 텍스트를 출력하는 과정에 대한 학습

## 🎉 print() 사용법

### 🎉 2.5.1

> 콤마(,)로 구분해서 출력하기

- 콤마(,)를 사용하면 공백으로 출력됨

### 🎉 2.5.2

> 키워드 sep으로 출력하기

- ex) sep="/" sep=""

### 🎉 2.5.3

> 문자열 포맷코드로 출력하기

- %d 정수형 숫자
- %f 실수형 숫자
- %.1f 실수에서 소숫점 첫째 자리(둘째 자리에서 반올림)
- %s 문자열

### 🎉 2.5.4

> 이스케이프 코드 출력하기

- \ + n 줄 바꿈
- \ + t 탭
- \ + \ 역슬래쉬 출력
- \+ ' 단 따옴표(') 출력
- \ + " 쌍따옴표 출력(")

### 🎉 2.6.1

> 파일로 프로그램 작성하기

- 메뉴 > file > save
- 단축키 Ctrl + S
- 단축키 F5 > 실행결과 출력 가능

### 🎉 2.6.2

> 탄생년을 입력받아 나이 계산하기

- 이름을 입력받아 변수 name에 저장
- 태어난 해를 입력받아 변수 birth에 저장
- **연도는 정수형 숫자로 처리해야 하기 때문에 int() 함수가 사용된다**
- age = 현재년 - 탄생년 + 1
- print(%s님의 나이는 %d세입니다."%(name,age)) 에서 %s는 문자열, %d는 정수형 숫자를 의미한다

### 🎉 2.7

> 주석문

- 프로그램을 짤 때 프로그램의 작성자, 작성한 날짜, 프로그램의 기능, 코드에 대한 주석(설명글)을 다는 데 사용되는 문장
- 여러 줄의 주석 처리할 경우 """ + """ / ''' + ''' 사용
- 샵(#)은 한줄의 주석 처리에 사용된다
- 주석 처리된 내용은 프로그램 실행 결과에 전혀 영향을 미치지 않는다

### 🎉 3.1

> 조건문

- 조건식의 참 또는 거짓에 따라 실행
- if ~ 구문, if ~ else ~ 구문, if ~ elif ~ else ~ 구문

### 🎉 3.2

> 비교 연산자와 논리 연산자

- 비교 연산자 - >,<, ==, !=, <=, >=
- 논리 연산자 - and, or, not
  - and - 조건 1,2 모두 참
  - or - 조건1, 2 둘 중에 하나만 참이어도 가능
  - not - 조건이 참이면 거짓, 거짓이면 참

### 🎉 3.3

> if ~ 구문

- if 조건식 :
  문장1
  문장2, ...
- if의 조건식이 참 -> 콜론(:) 다음 줄에 들여쓰기 되어 있는 문장1, 문장2 ... 을 수행

### 🎉 3.4

> if ~ else ~ 구문

- if 조건식 :
  문장1
  문장2, ...
  else :
  문장A
  문장B, ...
- if 다음의 조건식이 참 -> 콜론(:) 다음 줄에 들여쓰기 되어 있는 문장1, 문장2 ... 을 수행
  그렇지 X -> else : 다음 줄에 있는 문장A, 문장B, ... 를 수행

### 🎉 3.5

> if ~ elif ~ else ~ 구문

- if 조건식 :
  문장1
  문장2, ...
  elif 조건식:
  문장A
  문장B, ...
  else :
  문장i
  문장ii, ...
- if의 조건식 참 -> 들여쓰기 되어있는 문장1, 문장2, ... 를 수행
  그렇지 X -> elif( else if 의 약어 그렇지 않고 만약 )의 조건식이 참 -> 문장A, 문장B, ... 를 수행
  그렇지 X -> 앞 조건식들이 모두 거짓 -> else 다음의 문장i, 문장ii, ... 를 수행

### 🎉 3.6

> if 문의 중첩

- if 조건식:
  <문장들>
  elif 조건식:
  if 조건식:
  <문장들>
  else 조건식:
  <문장들>
  else :
  <문장들>
- if ~ 구문, if ~ else ~ 구문, if ~ elif ~ else ~ 구문

  ### 🎉 4.2.1

  > 반복문

- for문
  '~하는 동안'의 의미를 가지고 있음
  for 변수 in rnage 반복 횟수의 범위 :

  sum = 0
  for in range(1,11): #i가 1~10의 값을 가지고 10번 반복
  sum=sum + i
  print("i의 값 : %d => 합계 : %d"%(i,sum))

  ### 🎉 4.2.2

  > range() 함수
  > for i in range(10):

      print(i,end="")   #줄 바꿈

  print()

- 서식
  for 변수 in range(종료값):
  문장1,문장2,...
  for 변수 in range(시작값,종료값):
  문장1,문장2,...
  for 변수 in rnage(시작값,종료값,증가\_감소):
  문장1,문장2,...

  ### 🎉 4.2.3

  > 배수 합계 구하기
  > sum = 0
  > for i in range(100,201,5): #i가 100~200(5씩 증가)의 값을 가짐
  > sum=sum+i
  > print("5의 배수의 합계:%d"%sum)

  ### 🎉 4.2.4

  > for 문에서 문자열 다루기
  > for 변수 in 문자열:
  > 문장1,문장2 ...

### 🎉 4.3

> 이중 for문
> for ~ :
> for - :

      print()

print()

### 🎉 4.4.1

> while문의 기본구조
> while 조건식:
> 문장1
> 문장2
> ...

변수*초기화 #while문의 조건식에서 사용되는 변수의 값을 초기화
while 조건식:
문장1
문장2
...
변수값*증가\_감소

### 🎉 4.4.2

> while문으로 배수 합계 구하기
> 합계를 의미하는 변수를 0으로 초기화 시키고 변수를 특정 숫자로 초기화 시킨다
> ->while 루프에서 변수는 특정 숫자에서 시작하여 n씩 증가하다(ex:조건식 i<=600)
> ->조건식이 참인 동안 반복 조건식이 거짓이 될 경우 멈춘다
> ->누적 합계로 합계를 의미하는 변수가 구해진다

### 🎉 4.4.3

> 모음 개수 구하기
> 문자열의 인덱스를 나타내는 변수를 0, 모음 개수를 의미하는 변수를 0으로 초기화한다
> ->ex:i<=len(S)-1 #len(s)는 문자열 s의 길이를 의미한다
> 변수 i 가 문자열의 길이에서 1을 뺀 숫자보다 작거나 같은 동안 while 루프 반복한다
> ->변수 i 는 0~len(s)-1 까지의 값을 갖는다 변수 i 는 문자열 s 의 인덱스로 사용된다
> ->if 문의 조건식에서 문자열의 각 문자를 의미하는 s[i]가 모음인지를 체크한다
> ->참인 경우 문장 수행한다
> ->s[i]를 실행 결과의 첫번쩨 줄에 나타난 것과 같이 화면에 출력한다
> ->실행 결과의 두번째 줄에 나타난 것과 같이 모음의 개수를 출력한다

### 🎉 4.5

> break문으로 빠져나가기
> 주로 if문과 같이 사용되어 반복 루프가 진행되는 동안 조건식을 만족하면 반복루프를 빠져나가게 한다

for 변수 in range():
문장1
문장2
...
if 조건식:
break
...

### 🎉 E4-4.py

> 5의 배수를 한 줄에 5개씩 출력하는 프로그램
> count=0 #i의 개수
> for i in range(1,101):

    if i%5==0:
        print(i,end=" ")  #다음줄로 넘어가지 않게 end=" " 써줌
        count=count+1  #i 개수 1씩 증가
        if count%5==0:  #i의 개수가 5의 배수가 되면(=5)
            print()

### 🎉 E4-6.py

> for문을 이용해 10 팩토리얼을 구하는 프로그램
> sum=1
> for i in range(1,10):

    sum=sum*i
    print("10!=",sum)

### 🎉 5-1-1.py

> 리스트 생성하기
> 리스트명=[요소1,요소2,요소3,...]

### 🎉 5-1-2.py

> 리스트에서 요소 추출하기
> print(**_[0]) 인덱스는 0부터
> print(_**[5]) 인덱스 5의 요소
> print(**_[2:6]) 인덱스 2부터 5까지
> print(_**[-3]) 뒤에서 3번째 요소
> print(\_\_\_[-4:-1]) 뒤에서 4번째부터 뒤에서 2번째 까지의 요소

### 🎉 5-2-1.py

> for문에서 리스트 사용하기
> 변수는 리스트 각 요소의 값을 가지고 for 반복 루프 진행됨
> for 변수 in 리스트명:

### 🎉 5-2-2.py

> while문에서 리스트 사용하기
> animals=["코끼리","호랑이","사슴","펭귄","여우"]
> i=0
> while i<len(animals):

    print(animals[i])
    i=i+1

### 🎉 5-3-1.py

> 리스트 요소 수정하기
> 리스트명[인덱스]=데이터

### 🎉 5-3-2.py

> 리스트 요소 추가하기
> **_.append() 리스트 _**의 제일 뒤에 () 안의 값을 추가

### 🎉 5-3-3.py

> 리스트 요소 삽입하기
> 리스트명.insert(인덱스번호,데이터)

### 🎉 5-3-4.py

> 리스트 요소 위치 찾기
> 리스트의 index() 메소드는 리스트의 특정 요소의 위치, 즉 인덱스 번호를 구하는 데 사용

### 🎉 5-3-5.py

> 리스트 요소 삭제하기

- 리스트명.remove(데이터)
  : 리스트명 내에 있는 요소의 값이 데이터인 요소를 리스트에서 삭제
- 리스트명.pop(인덱스번호)
  : 인덱스번호가 지시하는 요소 값을 얻은 다음 해당 요소를 리스트에서 삭제
- 리스트명.clear()
  : 리스트명 내에 있는 모든 요소를 삭제

  ### 🎉 5-4-1.py

  > 리스트 병합하기
  > 리스트명=리스트1+리스트2+...

  ### 🎉 5-4-2.py

  > 리스트 합계 구하기
  > sum(리스트명)

        리스트명에 있는 요소들의 합계를 구함

  ### 🎉 5-4-3.py

  > 리스트 순서 반대로 하기
  > 리스트명.reverse()

       리스트명 내에 있는 요소들의 순서를 반대로 만듬

  ### 🎉 5-4-5.py

  > 리스트 정렬하기
  > 리스트명.sort()

        리스트명 내에 있는 요소돌을 오름차순으로 정렬

  ### 🎉 5-4.py 정리

  > 리스트 메소드
  > append() 리스트의 제일 뒤에 새로운 요소 추가
  > insert() 리스트에서 특정 인덱스 앞에 새로운 요소를 삽입함
  > index() 리스트에서 특정 요소의 위치인 인덱스 번호를 구함
  > remove() 리스트에서 특정 값을 가진 요소를 삭제함
  > pop() 리스트에서 특정 인덱스 번호를 사진 요소를 추출하고 그 요소를 리스트에서 삭제함
  > clear() 리스트 전체 요소를 삭제함
  > reserve() 리스트 요소들의 순서를 거꾸로 함
  > copy() 리스트를 복사하여 새로운 리스트를 생성함
  > sort() 리스트 요소들을 오름차순/내림차순으로 정렬함
  > 파이썬의 내장함수
  > list() 새로운 리스트를 생성함
  > len() 리스트의 길이를 구함
  > sum() 리스트 요소들의 합계를 구함

  ### 🎉 5-5-1.py

  > 문자열 찾기
  > find() 메소드 이용해 문자열 내에서 특정 문자 찾기
  > **_.find("~") : 문자열 _**에서 "~"이 제일 먼저 나오는 위치(인덱스번호값)

  ### 🎉 5-5-2.py

  > 문자열 치환하기
  > replace() 메소드 이용해 문자열 내에 있는 특정 문자열을 다른 문자열로 치환

  ### 🎉 S5-1.py

  > split
  > split() 메소드는 문자열에 있는 특정 문자를 기준으로 문자열을 분리하는 데 사용
  > **_.split("")는 공백("")을 기준으로 문자열 _**을 쪼갠 다음 리스트 형태로 저장

  ### 🎉 6-1-1.py

  > 튜플 생성하기
  > 튜플명=(데이터,데이터,데이터,...)
  > 리스트와 유사함 차이점으로는 -튜플에서는 리스트의 대괄호([]) 대신 소괄호(()) 사용 -튜플에서는 리스토와는 달리 요소의 수정과 추가가 불가능 -튜플에서는요소의 항목을 수정할 수 없음

  ### 🎉 6-1-2.py

  > 튜플 요소 추출하기
  > 튜플에서 리스트(또는 문자열)에서와 같은 방법으로 인덱스를 이용하여 요소를 추출할 수 있음
  > (튜플에서 인덱스는 0부터 시작)

  ### 🎉 6-1-3.py

  > 튜플 길이 구하기
  > len() 함수 이용 for i in range(len(\_\_\_)): for 루프가 튜플의 길이만큼 반복

  ### 🎉 6-1-4.py

  > 튜플 병합하기
  > 튜플=튜플1+튜플2+튜플3 <- 튜플1~3 하나로 합쳐서 튜플에 저장

  ### 🎉 6-1-4.py

  > 튜플 병합하기
  > 튜플=튜플1+튜플2+튜플3

  ### 🎉 6-1-5.py

  > 튜플에 관리자 정보 저장하기
  > **_=(...)
  > 튜플 _**에 ... 를 저장
  > 튜플의 인덱스를 이용하여 실행결과에서와 같이 각 요소 추출 가능

  ### 🎉 6-2-1.py

  > 딕셔너리 생성하기
  > 딕셔너리={키:값,키:값,키:값,키:값}
  > 딕셔너리에서는 키와 값으로 구성된 전체 요소를 추출

  ### 🎉 6-2-2.py

  > 딕셔너리 요소 추출하기
  > user={"id":"kim55","name":"강성준","level":7,"point":"10000"}
  > print(user) #딕셔너리 전체 출력
  > print(user["id"]) #"id"키의 값 출력
  > print(user["name"]) #"name"키의 값 출력
  > print(user["point"]) #"point"키의 값 출력

  ### 🎉 6-3-1.py

  > 딕셔너리에 요소 추가하기
  > 딕셔너리[키]=값
  > 딕셔너리 생성
  > 요소에 값 저장
  > 딕셔너리에서는 키가 존재하지 않기 때문에 키와 대응되는 값이 딕셔너리의 새로운 요소로 추가

  ### 🎉 6-3-2.py

  > 딕셔너리에 요소 수정하기
  > 딕셔너리[키]=값
  > 딕셔너리 생성
  > 생성한 딕셔너리의 키에 문자열 저장
  > 실행결과에서 키에 대응하는 값이 문자열 변경

  ### 🎉 6-3-3.py

  > 딕셔너리에 요소 삭제하기
  > -\_\_\_("+++")는 키의 값을 얻은 다음 저장
  > 동시에 키 "+++"의 요소 딕셔너리에서 삭제
  > 딕셔너리를 출력해보면 목록에서 "+++"의 키와 값이 삭제

-**_.clear()는 딕셔너리 _**의 전체 요소를 삭제
실행결과에 나타난 {}는 빈 딕셔너리 의미
딕셔너리 \_\_\_의 요소가 하나도 존재하지 않는다는 것을 나타냄

### 🎉 6-4.py

> for문과 딕셔너리
> for 변수 in 딕셔너리:
> ...
> 딕셔너리[변수]

for 루프에서 사용되는 변수는 딕셔너리의 키가 되고, 딕셔너리[변수]는 딕셔너리의 해당키에 대응되는 값

### 🎉 7-1-1.py

> 함수 정의와 호출
> hello() 함수 호출->정의된 hello() 함수 실행->hello() 함수 재호출->정의된 hello() 함수 실행->hello() 함수 재호출->정의된 hello() 함수 실행

def 함수명(): #함수정의
문장1
문장2
...
...
함수명() #함수 호출
...
함수명() #함수 호출
...

'define(정의하다)'의 약어, 함수를 정의한다는 의미 함수명()는 함수의 이름을 의미, (:) 다음 줄에 들여쓰기로 되어있는 문장은 함수의 기능을 뜻함

함수명() 함수 호출->정의된 함수명() 함수 실행->함수명() 함수 재호출->정의된 함수명() 함수 실행->함수명() 함수 재호출->정의된 함수명()함수 실행

### 🎉 7-1-2.py

> 함수의 종류
> print() 화면에 데이터 값을 출력함
> input() 키보드를 통해 데이터를 입력 받음
> range() 정수의 범위를 설정함
> list() 리스트를 생성함
> int() 문자열이나 실수형 숫자를 정수형 숫자로 변환함
> float() 문자열이나 정수형 숫자를 실수형 숫자로 변환함
> str() 정수형 숫자나 실수형 숫자를 문자열로 변환함
> type() 데이터의 형을 구함

### 🎉 7-2-1.py

> 매개변수란
> 매개변수:호출 함수의 인수를 정의 함수에서 전달 받기 위해 사용되는 변수
> 인수:호출 함수에서 정의 함수의 매개변수에 값을 전달하기 위해 사용되는 데이터나 변수

def say_hello(name): #name:매개변수
...
say_hello("홍지수"): #홍지수:인수
...

### 🎉 7-2-2.py

> 매개변수와 인수
> 호출 함수의 인수 개수와 정의 함수에서 사용되는 매개변수의 개수는 같아야함

### 🎉 7-2-3.py

> 매개변수의 유효 범위
> 정의 함수에서 사용되는 매개 변수는 정의된 함수 내에서만 유효
> \_\_\_() 함수 정의->메인루틴

### 🎉 7-2-4.py

> 매개변수 *args
> 하나의 함수에서 매개변수의 개수를 고정하지 않고 호출함수에서 전달하는 인수의 개수에 따라 매개변수를 가변적으로 하고 싶은 경우에 사용하는 것이 매개변수 *args 반드시 args란 매개변수 이름을 사용할 필요 X

### 🎉 7-2-5.py

> 매개변수에 리스트 전달하기
> 리스트생성->리스트를 인자로 하여 함수 호출->정의 함수에서는 호출 함수로부터 리스트가 저장된 컴퓨터 메모리의 주소를 전달받아 매개변수에 저장
> def +++(!!! #매개변수):

      ...

+++(??? #인수)
...

매개변수(parameter):호출 함수의 인수를 정의 함수에서 전달받기 위해 사용되는 변수
인수(argument):호출 함수에서 정의 함수의 매개변수에 값을 전달하기 위해 사용되는 데이터나 변수

### 🎉 7-2-2.py

> 매개변수와 인수
> 매개변수와 인수의 개수가 일치하지 않으면 오류 발생
> 함수에서 매개변수가 n개일 경우 호출하는 함수에서도 반드시 인수의 개수가 n개여야 함

### 🎉 7-2-3.py

> 매개변수의 유효범위
> 정의 함수에서 사용되는 매개변수는 정의된 함수 내에서만 유효
> 매개변수를 함수 호출 부분인 메인 루틴에서 사용하려고 하면 오류 발생

### 🎉 7-2-4.py

> 매개변수 *args
> 일반적인 함수 정의 에서 매개변수의 개수가 고정
> 그러나 하나의 함수에서 매개변수의 개수를 고정하지 않고 호출 함수에서 전달하는 인수의 개수에 따라 매개변수를 가변적으로 하고 싶은 경우 사용하는 것이 매개변수 *args

반드시 args란 매개변수 이름을 사용할 필요는 없음
\*scores와 같이 다른 이름을 사용해도 무방
하지만 호출 함수에서의 인수들은 scores의 튜플로 전달

### 🎉 7-2-5.py

> 매개변수에 리스트 전달하기
> 레퍼런스(reference)는 데이터가 저장된 메모리 번지의 주소를 의미 \*데이터가 저장된 메모리 주소

### 🎉 7-3.py

> 함수 값의 반환
> def 함수(매개변수1,매개변수2,...):
> 문장1
> 문장2
> ...
> return 변수1
> ...
> 변수2=함수(입력값,입력값2,...)
> ...

### 🎉 7-4-1.py

> 지역 변수
> 정의된 함수 내에서 사용되는 변수

### 🎉 7-4-2.py

> 전역 변수
> 메인루틴의 제일 앞에 정의된 변수
> 메인루틴과 정의된 함수 내에서 사용 가능

### 🎉 8-1.py

> 파이썬의 내장 함수
> print() 데이터나 변수를 화면에 출력함
> input() 키보드로 데이터를 입력 받음
> type() 변수의 데이터 형을 구함
> int() 실수나 문자열을 정수로 변환함
> float() 정수나 문자열을 실수로 변환함
> str() 정수나 문자열을 실수로 변환함
> sum() 리스트나 튜플 요소들의 합을 구함
> len() 문자열, 리스트, 튜플, 딕셔너리의 길이를 구함
> range() 10진 정수를 2진수로 변환함
> list() 새로운 리스트를 생성함
> tuple() 새로운 튜플을 생성함
> dict() 새로운 딕셔너리를 생성함
> ord() 문자의 아스키 코드 값을 구함
> bin() 10진 정수를 2진수로 변환함
> hex() 10진 정수를 16진수로 변환함
> round() 반올림 값을 구함
> max() 리스트나 튜플의 최댓값을 구함
> min() 리스트나 튜플의 최솟값을 구함

### 🎉 8-1-1.py

> 아스키 코드 구학-ord()
> 아스키코드:문자열 저장하는 데 사용되는 컴퓨터코드(0과 1의 조합)
> ord():문자의 아스키 코드 값을 구함

### 🎉 8-1-2.py

> 16진수/2진수 변환하기-hex(),bin()
> hex():10진 정수를 16진수로 변환함
> bin():10진 정수를 2진수로 변환함

### 🎉 8-1-3.py

> 반올림값 구하기-round()
> round():실수의 반올림 값을 구함

### 🎉 8-1-4.py

> 최댓값/최솟값 구하기-max(),min()
> max():리스트나 튜플의 최댓값을 구함
> min():리스트나 튜플의 최솟값을 구함

### 🎉 8-2-1.py

> 소수 여부 판별하기
> 함수 호출->소수O->True 값 얻고 그렇지 않으면 False

### 🎉 8-2-2.py

> 세제곱 합계 구하기
> def square_sum(n):

    sm=0
    for i in range(1,n+1):   #i:1~n
        sm=sm+(i*i*i)   #누적합계 sm 구함
    return sm

N=int(input("N의 값을 입력하세요"))
print(square_sum(N))

### 🎉 8-2-3.py

> 회문인지 판별하기
> 회문:거꾸로 읽어도 똑같은 단어나 문장
> for i in range(0,int(len(s)/2)): #문자열의 좌측 절반 읽음

      if s[i]!=s[len(s)-i-1]:         #좌측과 우측 인덱스 다른지 비교

<<<<<<< HEAD

### 🎉 9-1-1.py

> 모듈 생성하고 불러오기
> import ~ 구문 : 9.1.2 절
> import ~ as ~ 구문 : 9.1.3 절
> from ~ import ~ 구문 : 9.1.4 절

### 🎉 9-1-2.py

> import ~ 구문
> 모듈 함수 add() 는 두 수의 덧셈 결과를 반환한다
> 모듈 함수 sub() 는 두 수의 뺄셈 결과를 반환한다

import ~ 구문 :
import 모듈명
...
모듈명.모듈함수명()
...

### 🎉 9-1-3.py

> import ~ as ~ 구문
> import 모듈명 as 별칭
> ...
> 별칭.모듈함수명()
> ...

### 🎉 9-1-4.py

> from ~ import 구문

from 모듈명 import 모듈 함수명 ,모듈함수명,...
...
모듈함수명()
...

### 🎉 9-2.py

> math 모듈

math.sin() sin() 값을 반환함
math.cos() cos() 값을 반환함
math.tan() tan() 값을 반환함
math.ceil() 실수 값을 무조건 올림한 정수 값을 반환
math.floor() 실수 값을 무조건 내림한 정수 값을 반환
math.fsum() 리스트,튜플 등의 합계를 구함
math.log() 자연 로그 값을 반환
math.log() 자연 로그 값을 반환
math.log10() 밑을 10으로 한 로그 값을 반환
math.pow() 거듭제곱 값을 반환
math.sqrt() 제곱근 값을 반환
math.pi 원주율 3.1315...

### 🎉 9-1-1.py

> 모듈 생성하고 불러오기
> impport ~ 구문:9.1.2절
> import ~ as ~ 구문:9.1.3절
> from ~ import ~ 구문:9.1.4절

### 🎉 9-1-2.py

> import ~ 구문
> import 모듈명 #모듈명.py 파일에 정의되어 있는 변수, 함수 등을 불러옴
> ...
> 모듈명.모듈함수명()
> ...

### 🎉 9-1-3.py

> import ~ as ~ 구문
> import 모듈명 as 별칭
> ...
> 별칭.모듈함수명()
> ...

### 🎉 9-1-4.py

> from ~ import ~ 구문
> from 모듈명 import 모듈함수명,모듈함수명,... #모듈 내에 있는 모듈 함수를 직접 불러올 수 있음
> ...
> 모듈함수명()
> ...

### 🎉 9-2.py

> math 모듈
> math.sin() sin() 값을 반환함
> math.cos() cos() 값을 반환함
> math.tan() tan() 값을 반환함
> math.ceil() 실수 값을 무조건 올림한 정수 값을 반환
> math.floor() 실수 값을 무조건 내림한 정수 값을 반환
> math.fsum() 리스트, 튜플 등의 합계를 구함
> math.log() 자연 로그 값을 반환
> math.log10() 밑을 10으로 한 로그 값을 반환
> math.pow() 거듭제곱 값을 반환
> math.sprt() 제곱근(Square Root) 값을 반환
> math.pi 원주율 3.1415...

### 🎉 9-3.py

> time 모듈
> time.time() UTC 표준 시를 기준으로 한 현재 시간을 구함
> time.gmitime() UDT 초 단위의 시간을 struct_time 구조로 변환함
> time.localtime() 현지 시간(local Time)을 구함
> time.ctime() UTC 초 단위의 시간을 문자열로 변환함
> time.strtime() 일시를 포맷 기호를 이용하여 특정 포맷으로 변환함
> time.sleep() 일정 시간만큼 지연시킴

### 🎉 9-3-1.py

> 현재 시간 구하기
> time.time() 함수

### 🎉 9-3-2.py

> 타임스탬프를 문자열로 변환하기
> 타임스탬프:시간을 계산하는 것
> time.ctime() 함수 이용

### 🎉 9-3-3.py

> 시간을 특정 포맷으로 변환하기
> time.strftime() 함수를 이용하여 현재 시간을 특정 포맷의 문자열로 변환
> time.time() 함수 즉 현재의 타임 스탬프에 대해 time.localtime() 으로 현지 시간 구해 tm에 저장
> time.strftime() 함수를 이용하여 실행 결과에서와 같은 포맷으로 시간을 화면에 출력

%Y - 네자리 년도 - ex:2020,2021,2023
%y - 두자리 년도 - ex:00,31,99
%m - 월 - ex:04,11,12
%d - 일 - ex:01,06,31
%A - 요일 - Sunday,Monday,Saturday
%a - 간략 요일 - ex:Sun,Mon,Sat
%H - 시(24시) - ex:01,02,23
%l - 시(12시) - ex:01,02,12
%p - AM 또는 PM - ex:AM,PM
%M - 분 - ex:01,02,59

### 🎉 9-3-4.py

> 시간 지연시키기
> time.sleep(시간)

### 🎉 9-4.py

> datetime 모듈
> datetime.timedelta() 일시에 대한 산술 연산이 가능하게 포맷 변환
> datetime.date.today() 오늘의 날짜 구함
> datetime.datetime.now() 현재의 날짜와 시간 구함
> datetime.datetime,strftime() 날짜와 시간을 포맷 기호로 특정 포맷으로 변환

### 🎉 9-4-1.py

> 날짜와 시간에 대한 산술 연산
> datetime.timedelta()

### 🎉 9-4-2.py

> 오늘 날짜 구하기
> date.today()
> datetime.date.today()

### 🎉 9-4-3.py

> 일주일 후의 날짜 구하기
> datetime.timedelta()
> datetime.date.today()

### 🎉 9-4-4.py

> 현재의 날짜와 시간 구하기
> datetime 모듈 이용
> today=datetime.now() #현재 날짜와 시간

### 🎉 9-5.py

> random 모듈
> random.random() 0에서 1 사이의 임의의 실수를 변환함
> random.uniform() 주어진 두 수 사이의 임의의 실수를 반환함
> random.randint() 주어진 영역 사이의 임의의 정수를 반환함
> random.choice() 리스트,튜플,문자열 등에서 임의로 선택한 요소를 반환함
> random.shuffle() 리스트를 임의의 순서로 섞음

### 🎉 9-6-1.py

> 게임 시작 시간 표시하기
> time.time() :현재 시간에 대한 타임스탬프를 구함
> time.localtimw() :현재 시간의 타임스탬프에서 현지 시간(local time)을 구해 current_time에 저장
> time.strftime() :실행 결과의 포맷으로 현재의 시간

### 🎉 9-6-2.py

> 주사위 던지기
> random.randint(a,b) :주사위 눈에 해당되는 a에서 b까지의 숫자 중 하나 발생
> time.sleep(n) :n초간 멈추게 함

### 🎉 9-6-3.py

> 승부 판정하기
> id~else~elif 구문 이용-> 승부결과 화면에 표시

### 🎉 9-6-4.py

> 프로그램 전체 소스
> time.time() time.localtime() 함수 이용하여 컴퓨터의 현재 시간을 가져와 실행 결과의 첫번째 줄에서와 같이 게임 시작 시간을 화면에 표시
> while 의 조건식을 true로 하여 while 루프를 무한반복
> 변수 again의 값이 y인지 체크하여 게임 계속되는지 판단
> 두개의 random.randit(a,b)은 주사위를 던진 랜덤 수 얻어 각각에 저장한 다음 주사위 눈의 숫자를 화면에 출력
> 나와 컴퓨터 중 누가 이겼는지 체크하여 그 결과를 실행 결과에서와 같이 출력
> again이 y가 아니면 break문에 의해 while 루프에서 빠져나감
> 사용자에게 물어보고 입력된 값을 again에 저장한 다음 while루프로 돌아감
> 게임 종료 시간을 실행 결과의 마지막 줄에 표시

### 🎉 10-1-1.py

> 텍스트 파일 쓰기
> 파일 객체=open(파일명,파일모드,인코딩)
> r :읽기모드, 파일을 읽을 때 사용
> w :쓰기모드, 파일에 데이터를 저장할 때 사용 \*파일 존재x->새로운 파일 생성, 해당 파일이 존재하면 기존 파일에 덮어씀
> a :추가모드, 기존에 파일에 데이터를 추가할 때 사용

### 🎉 10-1-2.py

> 텍스트 파일 읽기
> f.readlines()는 new_file.txt의 내용 전체를 읽어서 변수 lines에 저장 변수 lines의 내용을 출력하면 실행 결과에서와 같이 출력 lines의 데이터형은 리스트가 됨 실행결과에 나타난 '|n'은 줄 바꿈을 의미

### 🎉 10-1-3.py

> 파일에서 성적 평균 구하기
> scores.txt 파일을 읽기 모드로 열어 파일 객체 file 생성 readlines() 함수로 파일의 데이터를 읽어들여 lines에 저장
> for 루프는 리스트 lines의 요소인 각 문자열 값을 가짐
> line.split() 함수는 리스트 line의 인덱스 0의 요소를 공백을 기준으로 분리하여 리스트 data에 저장 line.split() 함수는 리스트 line의 각 요소를 분리하여 리스트 data에 저장

### 🎉 10-1-4.py

> 파일과 폴더 삭제하기
> os 모듈 불러오기
> os.path.exist() 함수를 이용하여 sample.txt 파일 존재하는지 확인
> 해당 파일이 존재하면 True 반환 그렇지 않으면 False 반환
> os.remove() 함수 이용하여 sample.txt 파일 삭제

### 🎉 10-2-1.py

> CSV 파일 읽기
> csv 모듈 불러오기
> open() 함수 이용해 weater.csv 파일을 읽기 모드로 열어 파일 객체 f에 저장
> csv.reader() 메소드를 이용하여 파일 객체 f에서 데이터를 읽어들여 lines에 저장
> for 문으로 lines에서 데이터를 한줄씩 읽은 변수 line을 실행 결과에서와 같이 출력
> f.close() 메소드는 파일 객체 f를 닫음
